/**
 * Blog aksan rengi
 *
 * Kategoriler artık birbirinden renkle ayrılmıyor: tüm blog yüzeyi tek bir
 * nötr ton kullanıyor. Ton `blog.css` içinde `--cat-accent` olarak tanımlı;
 * burada yalnızca OG görselinin ihtiyaç duyduğu sayısal değer duruyor.
 */

/** Marka kırmızısıyla çakışmayan düşük doygunluklu slate. */
export const BLOG_ACCENT_HUE = 215;
